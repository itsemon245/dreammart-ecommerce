import React from 'react'

export default function Favorites({categories, auth }) {
    return (
        <CommonLayout categories={categories} pageTitle="Favorites" auth={auth} >


        </CommonLayout>
    )
}
